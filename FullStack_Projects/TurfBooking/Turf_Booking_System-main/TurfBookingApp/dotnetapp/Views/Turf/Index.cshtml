@model List<Turf>

<h1>Turf Reservation</h1>

<table class="table">
    <thead>
        <tr>
            <th>Turf ID</th>
            <th>Name</th>
            <th>Capacity</th>
            <th>Availability</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var turf in Model)
        {
            <tr>
                <td>@turf.TurfID</td>
                <td>@turf.Name</td>
                <td>@turf.Capacity</td>
                <td>
                    @if (turf.Availability)
                    {
                        <span class="text-success">Available</span>
                    }
                    else
                    {
                        <span class="text-danger">In Maintenance</span>
                    }
                </td>
                <td>
                    @if (turf.Availability)
                    {
                        <a id="bookButton" href="@Url.Action("Book","Booking", new { turfId = turf.TurfID })" class="btn btn-primary">Book</a>
                    }
                    <a id="deleteButton" href="@Url.Action("Delete","Turf", new { turfId = turf.TurfID })" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
